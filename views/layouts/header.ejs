<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Webshop</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light d-flex flex-column min-vh-100">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
    <a class="navbar-brand fs-3 fw-bolder text-uppercase text-light" href="/">Webshop</a>        
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link <%= currentPath === '/' ? 'active fw-bold' : '' %>" href="/">Összes termék</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= currentPath === '/instock' ? 'active fw-bold' : '' %>" href="/instock">Raktáron lévők</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= currentPath === '/new' ? 'active fw-bold' : '' %>" href="/new">Új termék</a>
                </li>
            </ul>
            
            <form class="d-flex me-3" action="/search" method="GET">
                <input class="form-control me-2 rounded-pill" type="search" name="query" placeholder="Keresés..." aria-label="Search">
                <button class="btn btn-outline-success rounded-pill" type="submit">Keresés</button>
            </form>


            <div class="navbar-nav">
                <a href="/cart" class="btn btn-outline-light position-relative rounded-pill"> 
                    <i class="bi bi-cart me-2 "> Kosár</i>
                    <% if (cart && cart.length > 0) { 
                        const itemCount = cart.reduce((total, item) => total + item.quantity, 0);
                    %>
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            <%= itemCount %>
                        </span>
                    <% } %>
                </a>
            </div>

        </div>
    </div>
</nav>

<div class="container">